# REALIA Browser MVP - Frontend (Vite + React)

## 起動
```bash
cd frontend
npm i
npm run dev
```

## 環境変数
`.env` を作るとバックエンドURLを変更できます。

```bash
VITE_BACKEND_URL=http://localhost:8787
```

## 通話（Phase1）
- DM画面の「通話（仮）」ボタンで外部通話ルームURL（Jitsi）が生成され、双方に表示されます。
